1.执行bash命令的4中方式：
    1.  bash 2.sh 会在当前父进程中产生生子进程，脚本执行完毕后，子进程会回到父进程中，不会影响当前环境
    2.  ./2.sh 1和2效果相同
    3.  source 2.sh 会在当前父进程中执行，不会产生子进程，会影响当前环境
    4.  . 2.sh 3和4效果相同
2.export 修饰的变量在当前shell和子shell是生效的，在另一个操作窗口是不生效的
3.PATH环境变量是，是命令搜索路径，使用了export修改，所以是对当前shell和当前子shell是生效的